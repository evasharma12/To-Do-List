<%- include("header") -%>
    <div class="heading">
        <h1 id="heading">
            <%= listTitle %>
        </h1>
        <div id="parah">
            <p>—How bad do you want it?</p>
        </div>

    </div>
    <div>
        <div class="table">
            <div class="list">

                <% newListItems.forEach(function (item){ %>
                    <form action = "/delete" method = "post">
                        <div>
                            <input type="checkbox" name = "checkbox" value = "<%=item._id%>"onchange="this.form.submit()">
                            <p>
                                <%= item.name %>
                            </p>
                        </div>
                    </form>

                    <% }); %>

            </div>

            <div class="input">
                <form action="/" method="post">
                    <div class="input-box">
                        <input id="input" type="text" name="newItem" placeholder="New item" autocomplete="off">
                    </div>

                    <button type="submit" class="btn" name="button" value=<%=listTitle%> >+</button>
                </form>
            </div>

        </div>
    </div>
    <%- include("footer") -%>